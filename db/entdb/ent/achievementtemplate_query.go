// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	uuid "github.com/gofrs/uuid/v5"
	"github.com/kozlov-ma/sesc-backend/db/entdb/ent/achievementgroup"
	"github.com/kozlov-ma/sesc-backend/db/entdb/ent/achievementtemplate"
	"github.com/kozlov-ma/sesc-backend/db/entdb/ent/predicate"
)

// AchievementTemplateQuery is the builder for querying AchievementTemplate entities.
type AchievementTemplateQuery struct {
	config
	ctx        *QueryContext
	order      []achievementtemplate.OrderOption
	inters     []Interceptor
	predicates []predicate.AchievementTemplate
	withGroup  *AchievementGroupQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AchievementTemplateQuery builder.
func (atq *AchievementTemplateQuery) Where(ps ...predicate.AchievementTemplate) *AchievementTemplateQuery {
	atq.predicates = append(atq.predicates, ps...)
	return atq
}

// Limit the number of records to be returned by this query.
func (atq *AchievementTemplateQuery) Limit(limit int) *AchievementTemplateQuery {
	atq.ctx.Limit = &limit
	return atq
}

// Offset to start from.
func (atq *AchievementTemplateQuery) Offset(offset int) *AchievementTemplateQuery {
	atq.ctx.Offset = &offset
	return atq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (atq *AchievementTemplateQuery) Unique(unique bool) *AchievementTemplateQuery {
	atq.ctx.Unique = &unique
	return atq
}

// Order specifies how the records should be ordered.
func (atq *AchievementTemplateQuery) Order(o ...achievementtemplate.OrderOption) *AchievementTemplateQuery {
	atq.order = append(atq.order, o...)
	return atq
}

// QueryGroup chains the current query on the "group" edge.
func (atq *AchievementTemplateQuery) QueryGroup() *AchievementGroupQuery {
	query := (&AchievementGroupClient{config: atq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := atq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := atq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(achievementtemplate.Table, achievementtemplate.FieldID, selector),
			sqlgraph.To(achievementgroup.Table, achievementgroup.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, achievementtemplate.GroupTable, achievementtemplate.GroupColumn),
		)
		fromU = sqlgraph.SetNeighbors(atq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AchievementTemplate entity from the query.
// Returns a *NotFoundError when no AchievementTemplate was found.
func (atq *AchievementTemplateQuery) First(ctx context.Context) (*AchievementTemplate, error) {
	nodes, err := atq.Limit(1).All(setContextOp(ctx, atq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{achievementtemplate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (atq *AchievementTemplateQuery) FirstX(ctx context.Context) *AchievementTemplate {
	node, err := atq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AchievementTemplate ID from the query.
// Returns a *NotFoundError when no AchievementTemplate ID was found.
func (atq *AchievementTemplateQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = atq.Limit(1).IDs(setContextOp(ctx, atq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{achievementtemplate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (atq *AchievementTemplateQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := atq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AchievementTemplate entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AchievementTemplate entity is found.
// Returns a *NotFoundError when no AchievementTemplate entities are found.
func (atq *AchievementTemplateQuery) Only(ctx context.Context) (*AchievementTemplate, error) {
	nodes, err := atq.Limit(2).All(setContextOp(ctx, atq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{achievementtemplate.Label}
	default:
		return nil, &NotSingularError{achievementtemplate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (atq *AchievementTemplateQuery) OnlyX(ctx context.Context) *AchievementTemplate {
	node, err := atq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AchievementTemplate ID in the query.
// Returns a *NotSingularError when more than one AchievementTemplate ID is found.
// Returns a *NotFoundError when no entities are found.
func (atq *AchievementTemplateQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = atq.Limit(2).IDs(setContextOp(ctx, atq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{achievementtemplate.Label}
	default:
		err = &NotSingularError{achievementtemplate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (atq *AchievementTemplateQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := atq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AchievementTemplates.
func (atq *AchievementTemplateQuery) All(ctx context.Context) ([]*AchievementTemplate, error) {
	ctx = setContextOp(ctx, atq.ctx, ent.OpQueryAll)
	if err := atq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AchievementTemplate, *AchievementTemplateQuery]()
	return withInterceptors[[]*AchievementTemplate](ctx, atq, qr, atq.inters)
}

// AllX is like All, but panics if an error occurs.
func (atq *AchievementTemplateQuery) AllX(ctx context.Context) []*AchievementTemplate {
	nodes, err := atq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AchievementTemplate IDs.
func (atq *AchievementTemplateQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if atq.ctx.Unique == nil && atq.path != nil {
		atq.Unique(true)
	}
	ctx = setContextOp(ctx, atq.ctx, ent.OpQueryIDs)
	if err = atq.Select(achievementtemplate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (atq *AchievementTemplateQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := atq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (atq *AchievementTemplateQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, atq.ctx, ent.OpQueryCount)
	if err := atq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, atq, querierCount[*AchievementTemplateQuery](), atq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (atq *AchievementTemplateQuery) CountX(ctx context.Context) int {
	count, err := atq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (atq *AchievementTemplateQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, atq.ctx, ent.OpQueryExist)
	switch _, err := atq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (atq *AchievementTemplateQuery) ExistX(ctx context.Context) bool {
	exist, err := atq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AchievementTemplateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (atq *AchievementTemplateQuery) Clone() *AchievementTemplateQuery {
	if atq == nil {
		return nil
	}
	return &AchievementTemplateQuery{
		config:     atq.config,
		ctx:        atq.ctx.Clone(),
		order:      append([]achievementtemplate.OrderOption{}, atq.order...),
		inters:     append([]Interceptor{}, atq.inters...),
		predicates: append([]predicate.AchievementTemplate{}, atq.predicates...),
		withGroup:  atq.withGroup.Clone(),
		// clone intermediate query.
		sql:  atq.sql.Clone(),
		path: atq.path,
	}
}

// WithGroup tells the query-builder to eager-load the nodes that are connected to
// the "group" edge. The optional arguments are used to configure the query builder of the edge.
func (atq *AchievementTemplateQuery) WithGroup(opts ...func(*AchievementGroupQuery)) *AchievementTemplateQuery {
	query := (&AchievementGroupClient{config: atq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	atq.withGroup = query
	return atq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AchievementTemplate.Query().
//		GroupBy(achievementtemplate.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (atq *AchievementTemplateQuery) GroupBy(field string, fields ...string) *AchievementTemplateGroupBy {
	atq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AchievementTemplateGroupBy{build: atq}
	grbuild.flds = &atq.ctx.Fields
	grbuild.label = achievementtemplate.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.AchievementTemplate.Query().
//		Select(achievementtemplate.FieldName).
//		Scan(ctx, &v)
func (atq *AchievementTemplateQuery) Select(fields ...string) *AchievementTemplateSelect {
	atq.ctx.Fields = append(atq.ctx.Fields, fields...)
	sbuild := &AchievementTemplateSelect{AchievementTemplateQuery: atq}
	sbuild.label = achievementtemplate.Label
	sbuild.flds, sbuild.scan = &atq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AchievementTemplateSelect configured with the given aggregations.
func (atq *AchievementTemplateQuery) Aggregate(fns ...AggregateFunc) *AchievementTemplateSelect {
	return atq.Select().Aggregate(fns...)
}

func (atq *AchievementTemplateQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range atq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, atq); err != nil {
				return err
			}
		}
	}
	for _, f := range atq.ctx.Fields {
		if !achievementtemplate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if atq.path != nil {
		prev, err := atq.path(ctx)
		if err != nil {
			return err
		}
		atq.sql = prev
	}
	return nil
}

func (atq *AchievementTemplateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AchievementTemplate, error) {
	var (
		nodes       = []*AchievementTemplate{}
		_spec       = atq.querySpec()
		loadedTypes = [1]bool{
			atq.withGroup != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AchievementTemplate).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AchievementTemplate{config: atq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(atq.modifiers) > 0 {
		_spec.Modifiers = atq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, atq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := atq.withGroup; query != nil {
		if err := atq.loadGroup(ctx, query, nodes, nil,
			func(n *AchievementTemplate, e *AchievementGroup) { n.Edges.Group = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (atq *AchievementTemplateQuery) loadGroup(ctx context.Context, query *AchievementGroupQuery, nodes []*AchievementTemplate, init func(*AchievementTemplate), assign func(*AchievementTemplate, *AchievementGroup)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*AchievementTemplate)
	for i := range nodes {
		fk := nodes[i].GroupID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(achievementgroup.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "group_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (atq *AchievementTemplateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := atq.querySpec()
	if len(atq.modifiers) > 0 {
		_spec.Modifiers = atq.modifiers
	}
	_spec.Node.Columns = atq.ctx.Fields
	if len(atq.ctx.Fields) > 0 {
		_spec.Unique = atq.ctx.Unique != nil && *atq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, atq.driver, _spec)
}

func (atq *AchievementTemplateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(achievementtemplate.Table, achievementtemplate.Columns, sqlgraph.NewFieldSpec(achievementtemplate.FieldID, field.TypeUUID))
	_spec.From = atq.sql
	if unique := atq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if atq.path != nil {
		_spec.Unique = true
	}
	if fields := atq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, achievementtemplate.FieldID)
		for i := range fields {
			if fields[i] != achievementtemplate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if atq.withGroup != nil {
			_spec.Node.AddColumnOnce(achievementtemplate.FieldGroupID)
		}
	}
	if ps := atq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := atq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := atq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := atq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (atq *AchievementTemplateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(atq.driver.Dialect())
	t1 := builder.Table(achievementtemplate.Table)
	columns := atq.ctx.Fields
	if len(columns) == 0 {
		columns = achievementtemplate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if atq.sql != nil {
		selector = atq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if atq.ctx.Unique != nil && *atq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range atq.modifiers {
		m(selector)
	}
	for _, p := range atq.predicates {
		p(selector)
	}
	for _, p := range atq.order {
		p(selector)
	}
	if offset := atq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := atq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (atq *AchievementTemplateQuery) ForUpdate(opts ...sql.LockOption) *AchievementTemplateQuery {
	if atq.driver.Dialect() == dialect.Postgres {
		atq.Unique(false)
	}
	atq.modifiers = append(atq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return atq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (atq *AchievementTemplateQuery) ForShare(opts ...sql.LockOption) *AchievementTemplateQuery {
	if atq.driver.Dialect() == dialect.Postgres {
		atq.Unique(false)
	}
	atq.modifiers = append(atq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return atq
}

// AchievementTemplateGroupBy is the group-by builder for AchievementTemplate entities.
type AchievementTemplateGroupBy struct {
	selector
	build *AchievementTemplateQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (atgb *AchievementTemplateGroupBy) Aggregate(fns ...AggregateFunc) *AchievementTemplateGroupBy {
	atgb.fns = append(atgb.fns, fns...)
	return atgb
}

// Scan applies the selector query and scans the result into the given value.
func (atgb *AchievementTemplateGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, atgb.build.ctx, ent.OpQueryGroupBy)
	if err := atgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AchievementTemplateQuery, *AchievementTemplateGroupBy](ctx, atgb.build, atgb, atgb.build.inters, v)
}

func (atgb *AchievementTemplateGroupBy) sqlScan(ctx context.Context, root *AchievementTemplateQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(atgb.fns))
	for _, fn := range atgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*atgb.flds)+len(atgb.fns))
		for _, f := range *atgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*atgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := atgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AchievementTemplateSelect is the builder for selecting fields of AchievementTemplate entities.
type AchievementTemplateSelect struct {
	*AchievementTemplateQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ats *AchievementTemplateSelect) Aggregate(fns ...AggregateFunc) *AchievementTemplateSelect {
	ats.fns = append(ats.fns, fns...)
	return ats
}

// Scan applies the selector query and scans the result into the given value.
func (ats *AchievementTemplateSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ats.ctx, ent.OpQuerySelect)
	if err := ats.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AchievementTemplateQuery, *AchievementTemplateSelect](ctx, ats.AchievementTemplateQuery, ats, ats.inters, v)
}

func (ats *AchievementTemplateSelect) sqlScan(ctx context.Context, root *AchievementTemplateQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ats.fns))
	for _, fn := range ats.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ats.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ats.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
