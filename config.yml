# This file is kept for local development only
# In production, all configuration comes from environment variables (kamal/secrets)

database:
  type: "postgres"
  address: "postgres://sesc:postgres@localhost:5432/sesc?sslmode=disable"

http:
  server_address: ":8080"
  read_header_timeout: 300ms
  read_timeout: 10s
  write_timeout: 10s

jwt_secret: "your_secret_key_here"

admin_credentials:
  - id: "f1157f63-65dc-4c3d-bcb2-4d6d55d2e3fd"
    username: "admin"
    password: "YUn4jX8/TpPRj0gfpqsP2g=="

minio:
  endpoint: "localhost:9000"
  access_key: "minioadmin"
  secret_key: "minioadmin"
  use_ssl: false
  bucket_name: "sesc-files"
  base_url: "http://localhost:9000/sesc-files"
